{namespace GSearchFacetSelections}

/**
 * Selected facets list.
 *
 * @param portletNamespace
 * @param? update
 *
 */
{template .render}
		<div class="col-xs-12 col-sm-12 col-md-10 col-lg-10  gsearch-centered gsearch-facet-selections" id="{$portletNamespace}facet-selections">
			<ul data-update="{$update}"></ul>			
		</div>
{/template}